# Authentication credentials configuration
# These values should be overridden via environment variables or an external
# secret store in production deployments.
auth.admin.key=${AUTH_ADMIN_KEY}
auth.root.key=${AUTH_ROOT_KEY}
